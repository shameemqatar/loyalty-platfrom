import { WhatsAppMessagingService } from './whatsapp-messaging.service.js';

describe('WhatsAppMessagingService', () => {
  it('should be defined', () => {
    expect(WhatsAppMessagingService).toBeDefined();
  });
});