import { WhatsAppMessagingController } from './whatsapp-messaging.controller.js';

describe('WhatsAppMessagingController', () => {
  it('should be defined', () => {
    expect(WhatsAppMessagingController).toBeDefined();
  });
});